import React from "react";
import PageHeader from "@/app/_components/PageHeader";
import Form from '@/components/ui/state/form'

const CreateStatePage = () => {
  return (
    <div>
      <PageHeader>Create State</PageHeader>
      <Form />
    </div>
  );
};

export default CreateStatePage;
